{
  "name": "ts-use",
  "private": true,
  "dependencies": {
    "multiformats": "file:../../dist/"
  },
  "scripts": {
    "test": "npm install && npm_config_yes=true npx -p typescript tsc --noEmit"
  }
}
