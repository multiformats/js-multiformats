{
  "name": "ts-use",
  "private": true,
  "dependencies": {
    "multiformats": "file:../../"
  },
  "scripts": {
    "test": "npm install && npm_config_yes=true npx -p typescript tsc --noEmit"
  },
  "eslintConfig": {
    "extends": "ipfs",
    "parserOptions": {
      "sourceType": "module",
      "project": [
        "./test/ts-use/tsconfig.json"
      ]
    }
  }
}